<mvc:View controllerName="project1.controller.Transactions"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <content>
        <!-- <Table id="booksTable" items="{path: '/Transactions', parameters: {expand: 'author'}}">
            <columns>
            
            <Column>
                <Text text="Name" />
            </Column>
            <Column>
                <Text text="Author" />
            </Column>
            
            
            <Column>
                <Text text="Total Copies" />
            </Column>
        </columns>
        
        <items>
            <ColumnListItem type="Navigation" press="onNavToTransactions">
                <cells>
                    
                    <ObjectIdentifier title="{name}" />
                    
                    
                   
                    <Text text="{author/name}" />
                    
                    
                    <Text text="{totalCopies}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table> -->
    </content>
    </Page>
</mvc:View>
